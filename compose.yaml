services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=runnerz'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=syn'
      - 'POSTGRES_HOST_AUTH_METHOD=password'
    ports:
      - '5432:5432'